const User = require('./user-schema');
const Message = require('./user_message');
const Photo = require('./photo_schema');
const Post = require('./post_schema');
const PostComment = require('./post-comment-schema');

module.exports = {
    User,
    Message,
    Photo,
    Post,
    PostComment,
}
